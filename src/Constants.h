#pragma once

#include <assert.h>
#include <iostream>

// window
const unsigned int WINDOW_WIDTH = 300;
const unsigned int WINDOW_HEIGHT = 300;

const float BRICK_SPEED = 45.0f;
const float BULLET_SPEED = 5.0f;

const unsigned int BRICK_SIZE = 10;
const unsigned int BULLET_SIZE = 5;

const unsigned int FRAME_TARGET_TIME = 60;
